package org.geonetwork.domain.ebrim.extensionpackage.basicextension.storedquery;

import org.geonetwork.domain.ebrim.informationmodel.core.Identifiable;

/**
 * 
 * Element to hold stored queries in GetRecords requests. The identifiable "id" is the id of the
 * store query; any parameters must be in Slots.
 * 
 * This class is used only in queries and therefore not persisted.
 * 
 * @author Heikki Doeleman
 * 
 */
public class StoredQuery extends Identifiable {
	public boolean equals(Object o) {
		return super.equals(o);
	}
	public int hashCode() {
		return super.hashCode();
	}
}
