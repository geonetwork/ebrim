package org.geonetwork.domain.filter110.expression;

public class PropertyName extends Expression {
	
	public boolean equals(Object o) {
		if(o instanceof PropertyName) {
			Expression base = (Expression) o;
			return super.equals(base);
		}
		else {
			return false;
		}
	}
	
	public int hashcode() {
		return super.hashCode();
	}

}
